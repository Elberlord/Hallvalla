Hallvalla - Sonido especial por rareza en apertura de paquetes 7HCH
============================================================

ZIP base: Hallvalla_add_pack_open_ogg_7HCG.zip
ZIP generado: Hallvalla_pack_rarity_sfx_7HCH.zip

Archivos agregados:
- assets/sfx/pack_special.ogg (13,971 bytes)
- assets/sfx/pack_demigod.ogg (12,932 bytes)

Cambio en script.js:
- Se agregó getPackRevealSound(cards).
- Se agregó playPackRevealRaritySound(cards).
- revealActivePack() ahora hace:
  1. tryPlaySound("pack_open")
  2. playPackRevealRaritySound(activePackCards)
  3. revela visualmente las cartas

Regla aplicada:
- Básica / mejorada: solo pack_open.
- Heroica, épica, gloriosa o mítica: pack_special.
- Legendaria o semidiós/demigod: pack_demigod.

Timing:
- El sonido especial entra 620 ms después de abrir el paquete, justo después del crujido inicial y cerca de la aparición visual de las cartas.

No se tocaron:
- Reglas, fases, mazos, IA o balance.
- CSS visual.
- Aros de convocación.
- FX de invocación.
- Brillo de rarezas.
- Assets de cartas, mapas o UI.

Verificación:
- script.js node --check: OK

