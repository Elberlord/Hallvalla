HALLVALLA · PVP REBUILD · PASO 6B
Fecha: 2026-08-14

Base: Paso 6A validado por el usuario en dos clientes.

Objetivo de 6B:
- mantener intacto el flujo lobby -> reglas -> LISTO -> Piedra/Papel/Tijera -> primero/segundo -> arena -> combate ACTIVE;
- habilitar únicamente el avance de fases canónicas desde el cliente que posee el turno;
- sincronizar cada transición mediante games/{code}/public/combatState;
- al terminar End Phase incrementar turnNumber e intercambiar activeRole/waitingRole;
- mantener bloqueados cartas, Honor, movimiento, ataques, casteos y efectos.

Secuencia de prueba 6B:
Inicio de turno -> Draw Phase -> Main Phase -> Action Phase -> Last Phase -> End Phase -> siguiente Turno / Inicio de turno.

Controles:
- solo el jugador activo ve habilitado SIGUIENTE FASE / TERMINAR TURNO;
- el jugador en espera no puede avanzar;
- ambos clientes renderizan la misma fase, turno y dueño desde Firebase.

Economía:
- sin cobros ni transferencias en 6B; apuestas económicas siguen diferidas hasta un bloque que también implemente rollback seguro.

Firebase Realtime Database Rules:
- SIN CAMBIOS respecto a Paso 6A.
