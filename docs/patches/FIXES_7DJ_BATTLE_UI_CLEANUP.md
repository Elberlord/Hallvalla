# HallValla v7DJ — Limpieza de interfaz de batalla

## Cambios
- Se ocultó el aviso permanente `TU TURNO · MAIN PHASE` del tablero.
- El aviso de fase queda como modal animado temporal (`phase-announce`) que aparece y desaparece.
- En PC, los botones Log / Configuración / Acciones se movieron al área negra izquierda para no tapar la arena.
- En móvil/tablet, esos controles quedan como encabezado compacto arriba.
- Se ajustó la intro del guardián para que el texto no arranque cortado arriba.
- Se modificaron `styles.css` e `index.html` para cache-buster.
